[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "all_os_commands"
version = "0.1.0"
description = "Cross-platform terminal helpers"
readme = "README.md"
requires-python = ">=3.7"